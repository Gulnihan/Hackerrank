if __name__ == '__main__':
    n = int(input())
    arr = list(map(int, input().split()))
    
sorted_arr=list(set(arr))

if len(arr) >= 2:
    sorted_numbers=sorted(sorted_arr)
    second = sorted_numbers[-2]
    print(second)
